<p id="notice"><%= notice %></p>

<p>
  <strong>Galaxy name:</strong>
  <%= @galaxy.galaxy_name %>
</p>
  
  <% @galaxy.planet.each do |f| %>
  <%= link_to "Planet[#{f.id}]", planet_path(f.id)%>
  <%= f.planet_name %> <br>
  <% end %>

<%= link_to 'Edit', edit_galaxy_path(@galaxy) %> |
<%= link_to 'Back', galaxies_path %>
