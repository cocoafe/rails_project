<%= form_for @alien do |f| %>
  <% if @alien.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alien.errors.count, "error") %> prohibited this alien from being saved:</h2>

      <ul>
      <% @alien.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :alien_name %><br>
    <%= f.text_field :alien_name %>
  </div>
  <div class="field">
    <%= f.label :category_id %>
    <%= f.select :category_id , options_for_select(Category.find(:all).collect{ |f| [f.alien_category, f.id] }) %>
  </div>
  <div class="actions">
  <%= f.submit %>
  </div>

<% end %>
